# Quantumult X 三池流量方案说明

本文说明当前 [`quantumult_20260409.conf`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf) 中已经落地的最新策略结构。目标是把机场职责拆清楚，避免后续换机场时需要重新整理整套策略组。

## 设计目标

当前方案只保留 3 个机场流量池：

1. `链式代理`
   作用：给美国住宅落地 IP 提供前置中间层。
   当前来源：`inet`
2. `中转`
   作用：作为链式代理的备用中间层。
   当前来源：`花云`
3. `直连`
   作用：承接国际媒体等不需要链式代理的常规国际流量。
   当前来源：`良心`

在这个结构下：

- `AI / Google / Gmail / Spotify` 这类对落地 IP 有要求的流量，统一走 `美国IP`
- `国际媒体` 统一走 `直连`
- `全球加速` 默认优先 `美国IP`，也允许手动切到 `直连` 或 `direct`
- `自动选择` 被移除，不再作为跨机场混合入口

## 当前配置骨架

链式代理骨架没有改动，仍然是：

```text
设备
-> 机场中间层（IP中转节点）
-> 落地IP-美国（204.252.84.84:47279）
-> 目标网站
```

对应配置位置：

- `[server_local]` 中的落地出口：
  [`quantumult_20260409.conf:84`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L84)
- `[filter_local]` 中把落地 IP 回送到中间层：
  [`quantumult_20260409.conf:159`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L159)

也就是说，当前方案改的是“谁来做中间层、谁来做直连”，不是改链式代理原理本身。

## 三个核心流量池

### 1. 链式代理

配置：

```ini
url-latency-benchmark=链式代理, resource-tag-regex=inet, server-tag-regex=^(?=.*(?:HK|SG|JP)\d{2})(?=.*5\.0x)(?=.*🌟).*$, check-interval=900, tolerance=0, alive-checking=false
```

含义：

- 只从 `inet` 订阅里取节点
- 只收 `HK / SG / JP` 三地
- 只收名称中包含 `5.0x` 和 `🌟` 的高倍率节点
- 用 `url-latency-benchmark` 自动测速，从候选节点里选最快的一个

当前正则：

```regex
^(?=.*(?:HK|SG|JP)\d{2})(?=.*5\.0x)(?=.*🌟).*$
```

命中示例：

- `HK05 / 5.0x🌟`
- `JP05 / 5.0x🌟`
- `SG03 / 5.0x🌟`

不命中示例：

- `HK08 / 0.2x / HKT`
- `JP08 / 0.5x`
- `US02🌟`

这样做的目的很明确：链式中间层只用你已经实测可用、且愿意为链式流量支付高倍率成本的节点，不把普通直连节点混进来。

### 2. 中转

配置：

```ini
url-latency-benchmark=中转, resource-tag-regex=花云, server-tag-regex=^(?=.*IEPL)(?=.*专线).*$, check-interval=900, tolerance=0, alive-checking=false
```

含义：

- 只从 `花云` 订阅里取节点
- 只要名称包含 `IEPL` 和 `专线`，就纳入备用池
- 当前不再细分香港、日本、新加坡，也不再区分“高级/实验性”

当前正则：

```regex
^(?=.*IEPL)(?=.*专线).*$
```

这样做的原因是：`花云` 在当前方案里不是日常主力，而是链式代理的备用入口。它的职责是“有备份可切换”，不是“做精细流量调度”。

### 3. 直连

配置：

```ini
url-latency-benchmark=直连, resource-tag-regex=良心, server-tag-regex=^(?=.*(?:香港|新加坡|日本|台湾)高速)(?=.*流媒体).*$, check-interval=900, tolerance=0, alive-checking=false
```

含义：

- 只从 `良心` 订阅里取节点
- 只收 `香港 / 新加坡 / 日本 / 台湾`
- 只收名称里带 `高速` 且带 `流媒体` 的节点
- 自动测速后选最快节点

当前正则：

```regex
^(?=.*(?:香港|新加坡|日本|台湾)高速)(?=.*流媒体).*$
```

命中示例：

- `香港高速01|BGP|流媒体`
- `新加坡高速03|BGP|流媒体`
- `日本高速02|BGP|流媒体`
- `台湾高速01|BGP|流媒体`

不命中示例：

- `美国高速01|流媒体`
- `香港专线01|BGP|流媒体`

这样做的原因是：`国际媒体` 不需要美国住宅落地，因此应该直接走稳定、低成本、低延迟的媒体节点，不占用链式资源。

## 链式代理入口

当前配置：

```ini
static=IP中转节点, 链式代理, 中转
static=美国IP, 落地IP-美国
```

含义：

- `IP中转节点` 只保留两个入口：
  - `链式代理`：默认主力
  - `中转`：手动备用
- `美国IP` 只负责引用本地落地节点 `落地IP-美国`

这意味着：

- 日常情况下，链式代理会先走 `inet`
- `inet` 出现问题时，你只需要把 `IP中转节点` 手动切到 `中转`
- 不需要再在 `美国节点 / 香港节点 / 日本节点 / 自动选择` 之间来回切换

## 业务策略如何引用三池

当前业务组配置如下：

```ini
static=苹果服务, direct, 直连, 美国IP, proxy
static=全球加速, 美国IP, 直连, direct, proxy
static=哔哩哔哩, direct, 直连, proxy
static=声田音乐, 美国IP, direct, 直连, proxy
static=国际媒体, 直连, direct, proxy
static=兜底分流, 全球加速, direct
```

对应策略意图：

| 策略组 | 默认优先级 | 设计意图 |
| --- | --- | --- |
| `苹果服务` | `direct -> 直连 -> 美国IP -> proxy` | Apple 业务优先直连，异常时再走机场 |
| `全球加速` | `美国IP -> 直连 -> direct -> proxy` | 普通国际站点默认优先链式，美国落地要求更稳定 |
| `哔哩哔哩` | `direct -> 直连 -> proxy` | 保持大陆优先，必要时可切外部节点 |
| `声田音乐` | `美国IP -> direct -> 直连 -> proxy` | 维持美区优先，不让媒体直连抢默认位 |
| `国际媒体` | `直连 -> direct -> proxy` | 不走链式代理，优先媒体直连 |
| `兜底分流` | `全球加速 -> direct` | 未命中规则的流量统一进入全球加速 |

这里最关键的是两点：

1. `国际媒体` 不引用 `美国IP`
2. `全球加速` 仍然把 `美国IP` 放在第一位

这和你的实际目标一致：需要美国落地的业务强制链式，不需要的媒体流量走直连。

## 远程规则如何落到这些策略组

`[filter_remote]` 当前绑定关系如下：

- `Ai-All-In-One` -> `美国IP`
- `Google` -> `美国IP`
- `GoogleSearch` -> `美国IP`
- `GoogleDrive` -> `美国IP`
- `GoogleCast` -> `美国IP`
- `GoogleFCM` -> `美国IP`
- `Google Voice` -> `美国IP`
- `Spotify音乐` -> `美国IP`
- `Gmail` -> `美国IP`
- `全球加速` 远程规则 -> `全球加速`
- `国际媒体` 远程规则 -> `国际媒体`
- `Apple` 远程规则 -> `苹果服务`

其中最重要的一条变化是：

```ini
https://raw.githubusercontent.com/ConnersHua/RuleGo/master/Surge/Ruleset/Proxy.list#via=%TUN%, tag=全球加速, force-policy=全球加速
```

这条规则不再直接写死 `美国IP`，而是先进入 `全球加速` 策略组。这样你仍然可以在策略组里根据场景手动切换到 `直连` 或 `direct`。

## 为什么删除“自动选择”

旧的“自动选择”模式本质上是在不同机场、不同地区、不同用途的节点之间做混合测速。这个方式在机场少的时候可以用，但维护成本高，问题也比较多：

- 链式代理和直连流量会抢同一个候选池
- 更换机场时，要同时改很多地区组
- 节点命名变化后，很难快速判断是哪一层出了问题

现在改成三池结构后，每个流量池只承担一种职责：

- `链式代理` 只负责 `inet`
- `中转` 只负责 `花云`
- `直连` 只负责 `良心`

这样出了问题就能快速定位，是链式主力坏了、备用坏了，还是媒体直连坏了。

## 后续更换机场时，只改这 4 处

后续如果你要替换机场，只需要检查和修改以下 4 处，不需要重构业务组：

1. `链式代理`
   改 `resource-tag-regex` 和 `server-tag-regex`
2. `中转`
   改 `resource-tag-regex` 和 `server-tag-regex`
3. `直连`
   改 `resource-tag-regex` 和 `server-tag-regex`
4. `IP中转节点`
   改主备顺序，决定默认先走哪一个中间层

业务组如 `全球加速 / 国际媒体 / 苹果服务 / 兜底分流` 可以保持不动。

## 维护建议

### 1. 先看池子是否收对节点

每次换机场后，先不要急着测网站，先在 QX 里看三个流量池是否收到了正确节点：

- `链式代理` 是否只出现 `inet` 的 HK/SG/JP 高倍率节点
- `中转` 是否只出现 `花云` 的 IEPL 专线
- `直连` 是否只出现 `良心` 的港/新/日/台流媒体节点

如果池子本身就为空，问题一定不在分流规则，而在订阅、命名或 regex。

### 2. 先验证三层，再验证业务

推荐顺序：

1. 验证 `链式代理` 是否能连通
2. 验证 `中转` 是否能作为备用连通
3. 验证 `直连` 是否能打开国际媒体
4. 最后再看 `Google / AI / Gmail / 国际媒体` 等业务规则是否命中正确策略

### 3. 不要把媒体节点混回链式池

`良心` 这次已经明确不适合作为当前住宅落地的中间层，因此不要再把它重新加入 `IP中转节点`。否则结构会重新退回到“混合候选池”，后续定位问题会再次变难。

## 对应配置位置

当前方案涉及的关键位置如下：

- 机场订阅：
  [`quantumult_20260409.conf:87`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L87)
  [`quantumult_20260409.conf:88`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L88)
  [`quantumult_20260409.conf:89`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L89)
- 核心流量池：
  [`quantumult_20260409.conf:115`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L115)
  [`quantumult_20260409.conf:116`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L116)
  [`quantumult_20260409.conf:117`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L117)
- 链式入口：
  [`quantumult_20260409.conf:120`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L120)
  [`quantumult_20260409.conf:121`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L121)
- 业务策略：
  [`quantumult_20260409.conf:124`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L124)
  [`quantumult_20260409.conf:129`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L129)
- 远程规则绑定：
  [`quantumult_20260409.conf:136`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L136)
  [`quantumult_20260409.conf:146`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L146)
- 本地链式回送：
  [`quantumult_20260409.conf:159`](/Users/jhx/Documents/Code/QuantumultX/quantumult_20260409.conf#L159)

## 结论

当前这份配置的核心，不是“按地区建很多组”，而是“按职责建 3 个池”：

- 链式主力用 `inet`
- 链式备用用 `花云`
- 国际媒体直连用 `良心`

这样做之后，机场和业务规则之间的耦合明显降低。后续你无论是替换 `良心`，还是把 `inet` 替换成别的直连 VLESS 机场，本质上都只是在替换流量池的节点筛选规则，而不是重写整套分流结构。
